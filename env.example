# ============================================================================
# CuraGenesis Intake CRM - Environment Variables Template
# ============================================================================
# Copy this file to .env and fill in your actual values
# NEVER commit .env to version control!
# ============================================================================

# ----------------------------------------------------------------------------
# DATABASE
# ----------------------------------------------------------------------------
# PostgreSQL (Production - Recommended)
DATABASE_URL="postgresql://user:password@localhost:5432/curagenesis_crm?schema=public"

# SQLite (Development/Local - Alternative)
# DATABASE_URL="file:./dev.db"

# ----------------------------------------------------------------------------
# CURAGENESIS API (Practice Intake Submission)
# ----------------------------------------------------------------------------
# Base URL for CuraGenesis practice intake API
CURAGENESIS_API_BASE="https://api.curagenesis.com"

# API key for authenticating intake submissions
CURAGENESIS_API_KEY="your_curagenesis_api_key_here"

# Vendor token for user creation and practice management
CURAGENESIS_VENDOR_TOKEN="your_vendor_token_here"

# Webhook secret for verifying real-time updates (optional)
CURAGENESIS_WEBHOOK_SECRET="your_webhook_secret_here"

# Request timeout in milliseconds (default: 10 seconds)
CURAGENESIS_API_TIMEOUT_MS="10000"

# Cron job secret for scheduled tasks (optional)
CRON_SECRET="your_cron_secret_here"

# ----------------------------------------------------------------------------
# CURAGENESIS METRICS API (KPI Dashboard)
# ----------------------------------------------------------------------------
# Base URL for CuraGenesis metrics/analytics API
NEXT_PUBLIC_CG_METRICS_BASE="https://api.curagenesis.com"

# Server-side API key for fetching KPI metrics
CG_METRICS_API_KEY="your_curagenesis_metrics_api_key_here"

# ----------------------------------------------------------------------------
# APPLICATION SETTINGS (Optional)
# ----------------------------------------------------------------------------
NODE_ENV="development"

# ----------------------------------------------------------------------------
# NOTES
# ----------------------------------------------------------------------------
# 1. If CG_METRICS_API_KEY is not set, dashboard will use mock data
# 2. If CURAGENESIS_API_KEY is not set, submissions will fail (expected in demo)
# 3. Database URL must be set for the application to start
# 4. Use PostgreSQL in production for better performance
# ============================================================================
